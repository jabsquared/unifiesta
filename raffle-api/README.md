# Just another Raffle API

## Installation

### Start the server:

* `npm install`
* `node index.js`

### Test locally

* `npm install -g pouchdb-server`
* `mkdir server && cd server`
* `pouchdb-server start`
* Search and install Fauxton
* Go to http://127.0.0.1:5984/_utils
